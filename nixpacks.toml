[phases.setup]
nixPkgs = ["python3", "postgresql"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[phases.build]
cmds = ["cd backend && python manage.py collectstatic --no-input"]

[start]
cmd = "cd backend && gunicorn hrms_backend.wsgi:application --log-file -"
